<odoo>
    <data>
        

        <!-- Acciones del Menú -->
        <record model="ir.actions.act_window" id="fleetx.orden_carga_action_window">
          <field name="name">Ordenes de Carga</field>
          <field name="res_model">fleetx.orden_carga</field>
          <field name="view_mode">tree,form</field>
        </record>
        <menuitem name="Ordenes de Carga" id="fleetx.orden_carga_menu" parent="fleetx.menu_root" action="fleetx.orden_carga_action_window"/>
    </data>
</odoo>